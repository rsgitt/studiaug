#include<stdio.h>
// do zrobienia
int main()
{
    int day, month, year;
    printf("Podaj dzien, miesiac i rok a ja sprawdze czy podane liczby\n");
    scanf("%d%d%d", &day, &month, &year);
    if ((year >= 1 && year <= 2023) )

}